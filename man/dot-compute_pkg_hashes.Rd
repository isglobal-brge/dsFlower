% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_store.R
\name{.compute_pkg_hashes}
\alias{.compute_pkg_hashes}
\title{Node-computed hashes of each top-level Python package in an unpacked app.
These are the trust anchors the integrity hook pins (the node computes them
itself, so a client cannot forge what is allowed to run).}
\usage{
.compute_pkg_hashes(apps_dir)
}
\description{
Node-computed hashes of each top-level Python package in an unpacked app.
These are the trust anchors the integrity hook pins (the node computes them
itself, so a client cannot forge what is allowed to run).
}
\keyword{internal}
