% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsi_transport.R
\name{flowerDsiStepDS}
\alias{flowerDsiStepDS}
\title{One federated logistic-regression gradient step (DSI transport, Phase 0)}
\usage{
flowerDsiStepDS(
  data_symbol,
  target,
  features_json,
  weights_json,
  stats_json = NULL,
  min_rows = 10L
)
}
\arguments{
\item{data_symbol}{Character; name of the data.frame symbol (e.g. "D").}

\item{target}{Character; binary 0/1 target column name.}

\item{features_json}{Character; JSON array of feature column names.}

\item{weights_json}{Character; JSON array of global weights (intercept first).}

\item{stats_json}{Character or NULL; JSON \code{{mu:[...], sd:[...]}} for
feature standardization (recommended for raw clinical scales).}

\item{min_rows}{Integer; refuse to compute on fewer rows (disclosure guard).}
}
\value{
Named list: \code{grad} (numeric, length = n_features+1) and \code{n}.
}
\description{
DataSHIELD AGGREGATE method. Loads the data symbol, optionally standardizes
features with the supplied global mean/sd, evaluates the logistic gradient at
the global weights over this site's rows, and returns the gradient SUM and the
(guarded) row count. The returned vector is an additive aggregate over the
site's samples; under the trust model it is the quantity SecAgg+ will later
mask so no single site's gradient is revealed.
}
\keyword{internal}
